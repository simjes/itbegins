---
import { Code } from 'astro:components';
// import type { Code as CodeType } from 'sanity.types';

// type Props = { node: Required<CodeType> };
const { node } = Astro.props;
---

<div class='mb-8'>
  <Code
    class='rounded-md border-2 border-cyan-500 p-4'
    code={node.code}
    lang={node.language}
    theme='synthwave-84'
  />
</div>
