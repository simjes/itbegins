---
import { Code as CodeInternal } from 'astro:components';
import type { Code } from 'sanity.types';

type Props = { node: Required<Code> };
const { node } = Astro.props;

// TODO: må gjøre migrering på dataen  BuiltinLanguage | SpecialLanguage | LanguageRegistration;
---

<CodeInternal code={node.code} lang={node.language} />
